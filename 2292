#define _CRT_SECURE_NO_WARNINGS
#include <stdio.h>

int main(void)
{
	int input, res, i;
	
	scanf("%d", &input);
	
	if(input == 1)
		res = 1;
	
	for (i = 1; input > 1; i++)
	{
		input -= 6 * i;
		res = i + 1;
	}
	printf("%d", res);
	return 0;
}
