#include <stdio.h>
#include <string.h>

int main(void)
{
	char str[1000001];
	int words = 0;
	
	scanf("%[^\n]", str);
	
	for(int i = 2; i<strlen(str); i++)
	{
		if(str[i-1] == 32)
		{
			words++;
		}
	}
	printf("%d\n", words+1);
	return 0;
}
