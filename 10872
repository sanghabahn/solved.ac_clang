#define _CRT_SECURE_NO_WARNINGS
#include <stdio.h>

int main(void)
{
	int input, count, res = 1;
	
	scanf("%d", &input);
	if (input == 0)
		printf("%d", res);
	
	else
	{
		for(count = 1; count < input; count++)
		{
			res *= count+1;
		}
		printf("%d", res);
	}
	return 0;
}
