#include <stdio.h>

int main(void)
{
    int arr[5];
    int sum = 0, res;
  	for (int i = 0; i < 5; i++)
  	{
    		scanf("%d", &arr[i]);
	  }
    
    for(int i = 0; i < sizeof(arr)/sizeof(int); i++)
    {
		    sum += (arr[i] * arr[i]);
    }
	
    res = sum % 10;
	  printf("%d\n", res);
    return 0;
}
