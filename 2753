#include <stdio.h>

int main(void)
{
	int year, res;
	scanf("%d", &year);
	
	if (((year % 4 == 0) && (year % 100 != 0)) || (year % 400 == 0))
		res = 1;
	else
		res = 0;
	printf("%d", res);
	return 0;
}
